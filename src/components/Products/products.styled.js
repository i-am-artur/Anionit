import styled from 'styled-components';
import { gap } from '../../common/Styles/theme';

export const Wrapper = styled.div`
	margin-top: ${gap.section};
`;

export const PropertyHeading = styled.h3`
	margin: 0;
`;

export const PropertyList = styled.ul`
	margin: 0;
	padding: inherit;
`;
